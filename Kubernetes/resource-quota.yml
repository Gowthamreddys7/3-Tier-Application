apiVersion: v1
kind: ResourceQuota
metadata:
  name: three-resourcequota
  namespace: three-tier

spec:
  hard:
    requests.cpu: "3"
    limits.cpu: "4"

    requests.memory: "3Gi"
    limits.memory: "4Gi"
