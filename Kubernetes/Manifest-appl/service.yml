apiVersion: v1
kind: Service
metadata:
  name: three-service
  namespace: three-tier

spec:
  type: ClusterIP

  selector:
    app: appln

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      
